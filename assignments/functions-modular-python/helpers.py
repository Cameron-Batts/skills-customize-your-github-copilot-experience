def greet_user(name):
    """Return a greeting message for the given name."""
    return f"Hello, {name}!"


def calculate_area(width, height):
    """Return the area of a rectangle."""
    return width * height


def format_result(name, area):
    """Return a formatted summary message."""
    return f"{name}, the area is {area:.2f}."
